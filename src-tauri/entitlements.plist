<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for hardened runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access for the GitHub / Jira integration API calls -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access for git operations -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!--
      No keychain-access-groups entitlement: the app is not sandboxed, so the
      `keyring` crate reaches the login keychain without one. The old
      $(AppIdentifierPrefix) value was an Xcode build-setting placeholder that
      codesign does not substitute, so signing would embed a literal invalid group.
    -->
</dict>
</plist>
