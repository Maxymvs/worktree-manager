# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode
.idea

.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target/
# NOTE: src-tauri/Cargo.lock is intentionally tracked. This is a binary, not a
# library, and the release workflow must resolve the exact same crate versions
# every build — otherwise a breaking transitive update fails a release at random.

# Build artifacts
*.dmg
*.app.zip

# Code signing
.env.signing
signing/
*.p12

# Test
test-results/
playwright-report/
